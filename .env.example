# استخدم هذا الملف كقالب وعدّل القيم حسب بيئة التشغيل
# انسخ الملف إلى .env: cp .env.example .env

# اختياري: عنوان API. في وضع التطوير (npm run dev) يمكن ترك المتغير فارغاً
# ليستخدم الفرونت تلقائياً /api عبر proxy في Vite → http://localhost:8000
# VITE_API_URL=http://localhost:8000/api
